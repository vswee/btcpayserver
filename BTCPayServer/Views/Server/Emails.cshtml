@model EmailsViewModel
@{
    ViewData.SetActivePageAndTitle(ServerNavPages.Emails);
    Layout = "_DashboardLayout";
    ViewData["Current"] = "ServerSettings";

}


<partial name="EmailsBody" model="@Model"/>
